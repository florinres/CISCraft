<Window x:Class="Ui.Views.Dialogs.MicroprogramSearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Search Label" 
        Height="150" 
        Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                  Text="Enter label to search:" 
                  Margin="0,0,0,8" 
                  Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        
        <!-- TextBox with placeholder effect using a TextBlock -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <TextBox 
                x:Name="SearchTextBox"
                Padding="8,6"
                KeyDown="SearchTextBox_KeyDown"
                ToolTip="Enter label name to search"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                
            <!-- Placeholder text that disappears when TextBox gets focus or has text -->
            <TextBlock 
                x:Name="PlaceholderText" 
                Text="Enter label name..." 
                Margin="8,6,0,0" 
                Foreground="Gray" 
                IsHitTestVisible="False"
                Visibility="{Binding ElementName=SearchTextBox, Path=Text.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
        
        <StackPanel Grid.Row="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right">
            <Button Content="Cancel" 
                   Margin="0,0,8,0"
                   Click="CancelButton_Click"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <Button Content="Search" 
                   IsDefault="True"
                   Click="SearchButton_Click"
                   Background="{DynamicResource AccentFillColorDefaultBrush}"
                   Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
        </StackPanel>
    </Grid>
</Window>