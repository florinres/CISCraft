<Window x:Class="Ui.Views.UserControls.Microprogram.GoToRowDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Go To Row" Height="200" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter row number:" Margin="0,0,0,12" FontSize="14"/>

        <TextBox Grid.Row="1" x:Name="RowTextBox" Margin="0,0,0,12" Padding="8,6" FontSize="14"
                 ToolTip="Enter row number (e.g. 42)"/>

        <TextBlock Grid.Row="2" x:Name="StatusTextBlock"
                   Foreground="Red"
                   Margin="0,0,0,12"
                   Visibility="Collapsed"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Cancel" Width="90" Height="32" Margin="0,0,12,0" 
                    Click="CancelButton_Click" IsCancel="True"/>
            <Button Grid.Column="2" Content="Go" Width="90" Height="32" 
                    Click="GoButton_Click" IsDefault="True"/>
        </Grid>
    </Grid>
</Window>